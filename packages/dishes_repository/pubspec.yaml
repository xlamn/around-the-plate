name: dishes_repository
description: A repository that handles dishes related requests.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  dishes_api:
    path: ../dishes_api

dev_dependencies:
  flutter_lints: ^5.0.0
  mocktail: ^1.0.0
  test: ^1.21.4