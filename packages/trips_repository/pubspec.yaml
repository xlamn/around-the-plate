name: trips_repository
description: A repository that handles trips related requests.
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.9.2 <4.0.0'

dependencies:
  trips_api:
    path: ../trips_api

dev_dependencies:
  flutter_lints: ^5.0.0
  test: ^1.21.4
